{
  "name": "tg-super-bus",
  "private": true,
  "packageManager": "pnpm@9.6.0",
  "workspaces": ["app", "worker"],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter app dev",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format"
  },
  "devDependencies": {
    "eslint": "^9.13.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  }
}